---
name: "Update external-attacher version"

sources:
  upstream:
    name: Get external-attacher version
    kind: githubrelease
    spec:
      owner: kubernetes-csi
      repository: external-attacher
      token: '{{ requiredEnv .github.token }}'
      typefilter:
        release: true
        draft: false
        prerelease: false
      versionfilter:
        kind: semver

targets:
  tagfile:
    name: "Bump to latest external-attacher version in TAG"
    kind: file
    scmid: default
    disablesourceinput: true
    spec:
      file: TAG
      matchpattern: '(?m)^(.+)$'
      replacepattern: '{{ source "upstream" }}'

scms:
  default:
    kind: github
    spec:
      token: '{{ requiredEnv .github.token }}'
      username: '{{ .github.username }}'
      user: '{{ .github.username }}'
      email: '{{ .github.email }}'
      owner: '{{ .github.owner }}'
      repository: '{{ .github.repository }}'
      branch: '{{ .github.branch }}'

actions:
  default:
    title: 'Bump external-attacher version to {{ source "upstream" }}'
    kind: github/pullrequest
    spec:
      automerge: false
    scmid: default
